# Runtime dependencies for the VGC MCP server.
#
# This file is kept in sync with pyproject.toml (the source of truth). It lists
# the core runtime deps PLUS the [remote] extra, because the hosted deployment
# (Render/Docker) runs the HTTP transport. For local/stdio use, the [remote]
# packages (uvicorn/starlette) are unused but harmless.
#
# Prefer `pip install -e ".[remote]"` (or ".[dev]" for development) over this
# file; it exists for platforms that only accept a requirements.txt.

# Core (pyproject [project].dependencies)
mcp>=1.0.0,<2.0  # 2.0 removed mcp.server.fastmcp; upgrading is a migration
httpx>=0.27.0
pydantic>=2.0.0
diskcache>=5.6.0
openpyxl>=3.1.0
fpdf2>=2.7.0

# HTTP transport (pyproject [remote] extra) — needed by vgc-mcp-http
uvicorn>=0.30.0
starlette>=0.38.0
